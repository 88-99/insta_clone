<%= form_with(model: user, local: true, url: confirm_user_path) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :profile_picture %>
    <%= image_tag(@user.profile_picture.url) if @user.profile_picture && @user.profile_picture.url %>
    <%= form.file_field :profile_picture %>
    <%= form.hidden_field :profile_picture_cashe %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end  %>
